import type { IShipType } from './types'

export const shipsSizes: Record<IShipType, number> = {
  xl: 4,
  lg: 3,
  md: 2,
  sm: 1,
}

export const shipsConfig: Record<IShipType, number> = {
  xl: 1,
  lg: 2,
  md: 3,
  sm: 4,
}
